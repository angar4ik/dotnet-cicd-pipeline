# Copy to .env for local overrides (never commit .env)
GIT_SHA=local
ASPNETCORE_ENVIRONMENT=Development
ConnectionStrings__Default=Host=localhost;Port=5432;Database=release_pipeline;Username=app;Password=app
